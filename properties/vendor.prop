# Audio
ro.hardware.audio.primary=taro-diting

# Fingerprint aidl
persist.vendor.fingerprint.type=udfps_optical
persist.vendor.fingerprint.sensor_location=610|2444|114

# Sensors
ro.vendor.sensors.xiaomi.udfps=true
